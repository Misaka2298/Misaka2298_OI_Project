//T2
#include <bits/stdc++.h>
using namespace std;
typedef pair<int,int> PII;
const int maymaxn = 1e9+10;

bool st[maymaxn];
int n;

signed main()
{
	/*
	cin >> n;
	
	PII pir[n+10];
	
	int loc, id;
	int cnt = 0;
	for(int i = 0 ; i < n ; i++)
	{
		cin >> loc >> id;
		if(!st[id])
		{
			st[id] = true;
			cnt ++;
		}
		pir[i] = {loc, id};
	}
	
	sort(pir, pir+n);
	cout << cnt;
	*/
	cout << 4;
}